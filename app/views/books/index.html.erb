<h2>Bookモデルの一覧画面</h2>
<!--一覧画面と新規投稿画面になるのでは<br>-->
<!--newとどちらを使用するか分からないから、newと同じフォームを書いておこう</p>-->


<h1>Books</h1>

<h3>Title</h3>
<h3>Body</h3>

<% @books.each do |book| %>
<!--titleっていう全体の表題があって、それぞれへ表題はなし-->
<!--カリキュラム３章では、<p>タグでタイトルと<span>タグで内容記述してる-->
  <p><%= book.title %></p>
  <p><%= book.body %></p>
  <p><%= link_to 'Show',"/books/#{book.id}" %></p>
  <p><%= link_to 'Edit',"/books/#{book.id}" %></p> <!--editのURLへリンクを変更する-->
  <p><%= link_to 'Destroy',"/books/#{book.id}" %></p> <!--destroyのリンクにする-->
<% end %>


<!--新規投稿newのビューと同様の記述-->
<h1>New book</h1>
<!-- form_with部分 -->
<%= form_with model:@book do |f| %>
  <h4>Title</h4>
  <%= f.text_field:title %>

  <h4>Body</h4>
  <%= f.text_area:body %>

  <%= f.submit 'Create BooK' %>

<% end %>